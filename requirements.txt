# Minimal Brain Requirements
# Tested with Python 3.13.5

# Core scientific computing
numpy==2.3.1

# GPU acceleration for similarity search
torch==2.7.1

# Optional: For visualization demos (if needed)
# matplotlib>=3.5.0

# Optional: For network communication (client-server mode)
# No additional requirements - uses built-in socket module

# Development tools (optional)
# pytest>=7.0.0  # For running tests