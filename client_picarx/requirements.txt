# PiCar-X Robot Requirements
# 
# NOTE: We do NOT need any SunFounder libraries!
# We control hardware directly via standard Raspberry Pi interfaces.

# Core requirements
numpy>=1.19.0

# Camera support (optional but recommended)
# Option 1: Raspberry Pi Camera
picamera2>=0.3.0  # For Raspberry Pi Camera Module

# Option 2: USB camera  
opencv-python>=4.5.0  # For USB cameras or advanced vision

# Audio support (optional but recommended)
pyaudio>=0.2.11  # For microphone and speaker
sounddevice>=0.4.0  # Alternative audio interface

# Pre-installed on Raspberry Pi OS:
# - RPi.GPIO (GPIO control)
# - smbus (I2C communication)
# - python3 standard library

# Installation on Raspberry Pi:
# sudo apt-get update
# sudo apt-get install python3-numpy python3-opencv python3-pyaudio
# sudo apt-get install libatlas-base-dev  # For numpy optimization